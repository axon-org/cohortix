name: DB Policy Guard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9'

jobs:
  # ═══════════════════════════════════════════════════════════════
  # DB POLICY GUARD
  # Enforces that every migration has RLS policies and that no
  # unapproved schema changes bypass row-level security checks.
  # ═══════════════════════════════════════════════════════════════

  db-policy-guard:
    name: DB Policy Guard
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Detect changed migration files
        id: changed-migrations
        run: |
          BASE_SHA=${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA=${{ github.sha }}

          # Fallback for manual dispatch or missing base
          if [ -z "$BASE_SHA" ]; then
            BASE_SHA="HEAD~1"
          fi

          CHANGED=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" -- 'supabase/migrations/*.sql' || true)
          # Use heredoc format for multiline GITHUB_OUTPUT values
          {
            echo "files<<EOF"
            echo "$CHANGED"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          if [ -n "$CHANGED" ]; then
            echo "Migration files changed:"
            echo "$CHANGED"
          else
            echo "No migration files changed."
          fi

      - name: Check migrations for RLS enable statements
        env:
          CHANGED_FILES: ${{ steps.changed-migrations.outputs.files }}
        run: |
          if [ -z "$CHANGED_FILES" ]; then
            echo "No changed migration files to check."
            exit 0
          fi

          echo "Checking RLS for changed files:"
          echo "$CHANGED_FILES"

          MISSING_RLS=0
          # Iterate over changed files (handling newlines)
          while IFS= read -r FILE; do
            [ -f "$FILE" ] || continue
            # Each migration that creates a table must have a matching ALTER TABLE … ENABLE ROW LEVEL SECURITY
            TABLES=$(grep -iE '^\s*CREATE TABLE' "$FILE" | sed -E 's/.*CREATE TABLE\s+(IF NOT EXISTS\s+)?("?[a-zA-Z0-9_]+"?\.)?("?[a-zA-Z0-9_]+"?).*/\3/I' || true)
            for TABLE in $TABLES; do
              if ! grep -qiE "ALTER TABLE.*${TABLE}.*ENABLE ROW LEVEL SECURITY" "$FILE"; then
                echo "❌  Missing ENABLE ROW LEVEL SECURITY for table '$TABLE' in $FILE"
                MISSING_RLS=1
              fi
            done
          done <<< "$CHANGED_FILES"

          # Also check for GRANT statements
          MISSING_GRANTS=0
          while IFS= read -r FILE; do
            [ -f "$FILE" ] || continue
            TABLES=$(grep -iE '^\s*CREATE TABLE' "$FILE" | sed -E 's/.*CREATE TABLE\s+(IF NOT EXISTS\s+)?("?[a-zA-Z0-9_]+"?\.)?("?[a-zA-Z0-9_]+"?).*/\3/I' || true)
            for TABLE in $TABLES; do
              CLEAN_TABLE=$(echo "$TABLE" | tr -d '"')
              if ! grep -qiE "GRANT.*ON.*${CLEAN_TABLE}.*TO.*authenticated" "$FILE"; then
                echo "❌  Missing GRANT for table '$TABLE' in $FILE"
                echo "    Add: GRANT SELECT, INSERT, UPDATE, DELETE ON public.${CLEAN_TABLE} TO authenticated;"
                MISSING_GRANTS=1
              fi
              if ! grep -qiE "GRANT.*ON.*${CLEAN_TABLE}.*TO.*service_role" "$FILE"; then
                echo "❌  Missing GRANT for service_role on table '$TABLE' in $FILE"
                echo "    Add: GRANT ALL ON public.${CLEAN_TABLE} TO service_role;"
                MISSING_GRANTS=1
              fi
              if grep -qiE "GRANT.*ON.*${CLEAN_TABLE}.*TO.*anon" "$FILE"; then
                echo "❌  Forbidden GRANT to anon role on table '$TABLE' in $FILE"
                echo "    anon role should not have direct table access. Remove the GRANT to anon."
                MISSING_GRANTS=1
              fi
            done
          done <<< "$CHANGED_FILES"

          FAILED=0
          if [ "$MISSING_RLS" -eq 1 ]; then
            echo ""
            echo "One or more tables are missing RLS. Add:"
            echo "  ALTER TABLE <table> ENABLE ROW LEVEL SECURITY;"
            FAILED=1
          fi

          if [ "$MISSING_GRANTS" -eq 1 ]; then
            echo ""
            echo "One or more tables are missing GRANT statements."
            echo "PostgREST will return 'permission denied' without them."
            FAILED=1
          fi

          if [ "$FAILED" -eq 1 ]; then
            exit 1
          fi

          echo "✅  All tables in migrations have RLS enabled and proper grants."

      - name: Lint SQL migrations (sqlfluff)
        run: |
          if ! command -v sqlfluff &> /dev/null; then
            pip install sqlfluff --quiet
          fi
          MIGRATION_DIR="supabase/migrations"
          if [ -d "$MIGRATION_DIR" ] && ls "$MIGRATION_DIR"/*.sql 1>/dev/null 2>&1; then
            sqlfluff lint --dialect postgres --exclude-rules L016,L034 "$MIGRATION_DIR" \
              || echo "⚠️  SQL lint warnings detected (non-blocking)."
          else
            echo "No SQL migration files to lint."
          fi
        continue-on-error: true
